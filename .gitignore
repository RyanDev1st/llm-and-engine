# CocoIndex Code (ccc)
/.cocoindex_code/

# Claude Code — local / ephemeral (do not commit)
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/worktrees/

# Secrets and local runtime
.env
.env.*
*.pem

# Large ML artifacts — never commit (base weights, runs, engines, GGUF)
src/llm/models/
src/llm/runtime/
runs/
*.gguf
*.safetensors
*.bak
ruvector.db
*.db

# Python caches
__pycache__/
*.pyc
