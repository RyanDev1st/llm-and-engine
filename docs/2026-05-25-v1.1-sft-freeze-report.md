Parent: docs/superpowers/plans/2026-05-25-v1.1-sft-data-quality.md

## Status

Frozen v1.1 SFT corpus passes generator, stratified split, validation, and stricter freeze audit gates.

## Scope

- Rebuilt `data/sft/v1_gold/accepted.jsonl` and `data/sft/v1_gold/rejected.jsonl` from the v1.1 generator.
- Rebuilt stratified `data/sft/v1_train.jsonl` and `data/sft/v1_val.jsonl`.
- Audited accepted coverage, rejected fixtures, slice minimums, synthetic-name share, rule coverage, reject diversity, and generic-final concentration.

## Evidence

- `$env:PYTHONPATH='src/llm'; python -m pytest src/llm/llm_dataset/v1/tests/test_audit.py -q`
  - Result: `4 passed in 0.20s`; tests first failed against weaker gates, then passed after tightening audit checks.
- `$env:PYTHONPATH='src/llm'; python -m pytest src/llm/llm_dataset/v1/tests/test_renderer_universality.py -q`
  - Result: `2 passed in 0.78s`; tests first caught universality boilerplate finals, then passed after slice-specific finals.
- `$env:PYTHONPATH='src/llm'; python -m pytest src/llm/llm_dataset/v1/tests/test_generate.py::test_audit_rejects_have_diverse_reasons -q`
  - Result: `1 passed in 0.11s`; test first caught reject monoculture, then passed after adding four reject fixture families.
- `$env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.generate`
  - Result: `wrote accepted=4447 rejected=800`.
- `$env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.build`
  - Result: `wrote train=3997 val=450`.
- `$env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.audit *> build/v1_audit.txt`
  - Result: exit code 0; `failures_count=0`; `freeze_ok=True`; no `MISSING` or `FAIL` lines.
  - Accepted by slice: A 632, B 390, C 291, D 324, E 356, F 326, G 155, H 223, I 428, J 292, K 190, each V1_A through V1_L 70.
  - Rule coverage: every rule has accepted coverage, including `skill_body_strict:4447`, `engine_grounded:3607`, `tool_text_is_data:70`, and `board_claim_grounded:70`.
  - Synthetic share: accepted 0.329, rejected 0.307.
  - Quality gates: `reject_reason_diversity=5`; `generic_final_share=0.000`.

## Next

1. Commit only intended v1.1 dataset, generator, audit, tests, and report files.
2. Use this stricter frozen corpus as the v1.1 SFT handoff artifact for the chess-coach harness work.
