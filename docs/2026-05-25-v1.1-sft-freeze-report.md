Parent: docs/superpowers/plans/2026-05-25-v1.1-sft-data-quality.md

## Status

Frozen v1.1 SFT corpus passes generator, stratified split, validation, and freeze audit gates.

## Scope

- Rebuilt `data/sft/v1_gold/accepted.jsonl` and `data/sft/v1_gold/rejected.jsonl` from the v1.1 generator.
- Rebuilt stratified `data/sft/v1_train.jsonl` and `data/sft/v1_val.jsonl`.
- Audited accepted coverage, rejected fixtures, slice minimums, synthetic-name share, and rule coverage.

## Evidence

- `$env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.generate`
  - Result: `wrote accepted=4446 rejected=800`.
- `$env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.build`
  - Result: `wrote train=3996 val=450`.
- `$env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.audit *> build/v1_audit.txt`
  - Result: exit code 0; `failures_count=0`; `freeze_ok=True`; no `MISSING` or `FAIL` lines.
  - Accepted by slice: A 632, B 390, C 291, D 323, E 356, F 326, G 155, H 223, I 428, J 292, K 190, each V1_A through V1_L 70.
  - Rule coverage: every rule has accepted coverage, including `skill_body_strict:4446`, `engine_grounded:3606`, `tool_text_is_data:70`, and `board_claim_grounded:70`.
  - Synthetic share: accepted 0.329, rejected 0.307.
- `$env:PYTHONPATH='src/llm'; python -m pytest src/llm/llm_dataset/v1/tests -q`
  - Result: `18 passed in 1.87s`.

## Next

1. Commit only intended v1.1 dataset, generator, audit, and report files.
2. Use this frozen corpus as the v1.1 SFT handoff artifact for the chess-coach harness work.
