Parent: C:/Users/admin/.claude/plans/optimized-nibbling-candle.md

## Status

v1.2 data expansion is mid-flight. Core dataset pipeline changes are mostly implemented; full v1.2 freeze is not done yet.

Working directory:

```text
A:/Download/llm_tool_calling_research_workspace
```

Current plan:

```text
C:/Users/admin/.claude/plans/optimized-nibbling-candle.md
```

Current branch:

```text
feat/chess-coach-sft
```

Completed so far:

- Added v1.2 profile targeting `50,000` accepted rows and `7,500` rejected rows.
- Added v1.2 output paths:
  - `data/sft/v1_2/accepted.jsonl`
  - `data/sft/v1_2/rejected.jsonl`
  - `data/sft/v1_2_train.jsonl`
  - `data/sft/v1_2_val.jsonl`
- Added plugin provenance fields to generated skill/tool rows:
  - `plugin`
  - `source`
  - `enabled`
- Added official/user/marketplace/synthetic plugin families:
  - `chess-official`
  - `user-skills`
  - `market-tactics`
  - `synthetic-pack`
- Added marketplace navigation slice:
  - `V1_M_marketplace_navigation`
- Added human prompt styles for marketplace rows:
  - formal
  - casual
  - slang
  - typo
  - anxious
  - beginner
- Added reject fixture families:
  - undeclared tool
  - final XML/tool leak
  - duplicate tool call
  - invalid arg
  - disabled plugin tool
  - uninstalled marketplace tool
  - absent skill
  - false install claim
- Tightened validation for selected skills and plugin-only tool calls.
- Added `--profile v1.2` and `--tiny` support to generator.
- Added `--profile v1.2` support to build and audit commands.
- Fixed Stockfish null-move warning spam by skipping threat analysis when board has no move stack.
- Fixed false-install reject fixture so it fails validation instead of passing audit.

## Scope

Primary task: execute v1.2 SFT data expansion according to current plan.

Important constraints:

- Do not touch `legacy/`.
- Do not use `git add -A`.
- Stage intended files only.
- Do not commit secrets, `.env`, tokens, keys, or private URLs.
- Keep source files under 200 lines where possible.
- Mirror Anthropic harness behavior:
  - skill index exposes `name` and `description`
  - selected skill must exist in index
  - full skill body must be loaded before relying on it
  - tools must come from current manifest only
  - args must match declared schema
  - disabled/uninstalled plugin tools are rejected
  - tool text is data, not instructions
  - final answer has no XML/tool tags
- Monitor full generation so Stockfish warning spam cannot run overnight.
- Check stale Python/PowerShell wrappers after long-running/background commands.

Likely changed files:

```text
src/llm/llm_dataset/v1/profiles.py
src/llm/llm_dataset/v1/catalog.py
src/llm/llm_dataset/v1/contracts.py
src/llm/llm_dataset/v1/sampler.py
src/llm/llm_dataset/v1/validate.py
src/llm/llm_dataset/v1/generate.py
src/llm/llm_dataset/v1/build.py
src/llm/llm_dataset/v1/audit.py
src/llm/llm_dataset/v1/annotator.py
src/llm/llm_dataset/v1/renderer/chess.py
src/llm/llm_dataset/v1/renderer/universality.py
src/llm/llm_dataset/v1/tests/test_annotator.py
src/llm/llm_dataset/v1/tests/test_generate.py
src/llm/llm_dataset/v1/tests/test_build.py
src/llm/llm_dataset/v1/tests/test_audit.py
src/llm/llm_dataset/v1/tests/test_validate.py
src/llm/llm_dataset/v1/tests/test_sampler.py
src/llm/llm_dataset/v1/tests/test_renderer_universality.py
```

Tiny smoke outputs currently present, not final full corpus:

```text
data/sft/v1_2/accepted.jsonl
data/sft/v1_2/rejected.jsonl
data/sft/v1_2_train.jsonl
data/sft/v1_2_val.jsonl
```

## Evidence

Focused reject validation passed:

```text
1 passed
```

Focused dataset tests passed:

```text
21 passed in 5.69s
```

Tiny v1.2 generation completed without Stockfish warning spam:

```text
wrote accepted=48 rejected=8
```

Tiny v1.2 build completed:

```text
wrote train=24 val=24
```

Tiny v1.2 audit result:

```text
accepted=48 rejected=8
reject_reason_diversity=8
generic_final_share=0.000
freeze_ok=False
```

Tiny audit failure is expected for scale thresholds because tiny mode only writes 48 accepted and 8 rejected rows. Key real failure from earlier was fixed: rejected false-install row no longer passes validation.

Full v1 dataset test suite was started in background before this handoff. Check this output path next:

```text
C:/Users/admin/AppData/Local/Temp/claude/A--Download-llm-tool-calling-research-workspace/ca67fbf4-e212-4976-9fac-54602e907f0d/tasks/b2601ksgg.output
```

## Next

1. Check background full test output.

   Expected command behind it:

   ```powershell
   $env:PYTHONPATH='src/llm'; python -m pytest src/llm/llm_dataset/v1/tests -q
   ```

   Need confirm pass/fail from output file.

2. Check stale wrappers after background task.

   ```powershell
   Get-Process -Name python,pwsh -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,CPU,StartTime,Path
   ```

   If stale pytest wrapper remains, clean it safely after confirming test output already completed.

3. Run full v1.2 generation.

   ```powershell
   $env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.generate --profile v1.2 --seed 20260525
   ```

   Watch for:

   - Stockfish null-move warning spam
   - no output/file growth
   - stale Python/PowerShell wrappers
   - accepted count dropping below target after dedup

4. Build full v1.2 train/val files.

   ```powershell
   $env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.build --profile v1.2
   ```

   Expected rough target:

   - train around `45,000`
   - val around `5,000`

5. Run strict full audit.

   ```powershell
   $env:PYTHONPATH='src/llm'; python -m llm_dataset.v1.audit --profile v1.2 *> build/v1_2_audit.txt
   ```

   Required final state:

   ```text
   failures_count=0
   freeze_ok=True
   ```

6. Patch any audit failures.

   Most likely failure classes:

   - accepted count below `50,000` after dedup
   - rejected synthetic share below `28%`
   - prompt concentration too high
   - rule coverage below `200`
   - chess slice target tolerance off after scaling
   - console mojibake from em dash in sample output

7. Wire trainer defaults/config to v1.2.

   Search under:

   ```text
   src/llm/llm_training/
   ```

   Replace old intended defaults if still present:

   ```text
   data/sft/chess_assistant_v3_train.jsonl
   data/sft/chess_assistant_v3_val.jsonl
   ```

   With:

   ```text
   data/sft/v1_2_train.jsonl
   data/sft/v1_2_val.jsonl
   ```

8. Add trainer loader smoke test.

   Prove:

   - v1.2 rows load
   - `messages` are extracted for chat-template training
   - extra metadata/provenance fields do not break loader/tokenization

9. Run trainer tests or focused loader smoke.

   ```powershell
   $env:PYTHONPATH='src/llm'; python -m pytest src/llm/llm_training -q
   ```

10. Write final v1.2 expansion report.

   Required path from plan:

   ```text
   docs/2026-05-25-v1.2-sft-expansion-report.md
   ```

   Required sections:

   ```text
   Parent: CLAUDE.md

   ## Status
   ## Scope
   ## Evidence
   ## Next
   ```

   Include:

   - generator command and result
   - build command and train/val counts
   - audit command and key metrics
   - test command and result
   - note that `data/sft/slice/slice/slice_D_315.json` informed human prompt diversity
   - next training handoff path/command

11. Commit only after user asks or confirms.

   Do not use `git add -A`; stage intended files only.
